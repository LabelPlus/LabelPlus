
v1.2.3
    修复在显示图片过长或过宽时，标签显示过大的问题。
    修正默认分组，1分组和8分组 颜色接近，容易混淆的问题
    修正快捷爱心符号`♥`与`♡`不匹配的问题

v1.2.2
    修复某些语言区域环境下，程序无法正常运行的问题

v1.2.1
    开始使用语义化版本号
    修复 标号数>=10时，显示不正常的问题(bug报告及协助测试:如雪凌尘☆镜月,ce肉球,Pxk888)

v1.2	
    导入图片支持tif格式
    修复竖屏模式最小化抛出异常(by sgqy)
    修复U盘存储时打字卡顿的问题(by sgqy)
    修复新建翻译时强行覆盖的问题
    修复编码问题"win下的Unicode默认为utf16 而labelplus文本编码为utf8" 
    -
    ps脚本部分:
    增加图源文件名按顺序替换的功能, 如"P021.jpg,P022.jpg"可以方便地替换成"img01.png,img02.png"
    增加_start _end动作的提示文本, 这两个动作分别在图片打开后/保存前被执行
    增加涂白功能选项
    修复MacOS(unix系)下路径分隔符号不兼容的问题
    修复执行动作导致报错的问题(bug报告:IS,黑翼天使,几千块,SPecik)
    修复若文档类型为索引色模式时报错的问题    
    在"使用指定类型图源"中增加.tif后缀等选项


v1.1 
    导出带标号图片 增加"不导出未标号图片"选项(by sgqy)
    修复用数字键设置分组后会自动选择列表中第一项的问题
    ps脚本.执行动作功能 增加特性 文件打开后执行"_start"动作 保存前执行"_end'动作
    ps脚本.修复加载配置时, 不加载动作分组的问题
	
v1.0 正式版
    增加标签列表框右键菜单
    打开文件时自动切换到第一张图片
    修复""换页时选择的标签未归零,输入译文的文本框内容未刷新"的问题
    PS脚本修复不读取最后一条Label的问题(byPxk888)
    PS脚本增加英语版本
	
v1.0 preview2
    将ps脚本导入功能删除，改为在ps脚本中读取LabelPlus文件，利于加入更多功能
    新增模式输入模式，切换选项时，浏览界面自动切换到对应标签所在位置
    新增模式审校模式，在标签上方显示分组名；鼠标移动到标签上，自动切换当前编辑的标签    
    新增C键在图片浏览界面临时显示标签分组名称的功能
    新增在列表框中按Del键删除若干个标签的功能
    增加配置文件，用户可修改各种默认设置    
    增加修改分组、修改备注信息的功能    
    新增缩放按钮、比例显示组合框
    改善QuickText功能，在图片浏览界面上按A，可快速插入标签（建议by Misaki）
    改善翻页速度、插入标签速度
    更改LabelPlus文件格式，将分组信息、备注储存在文件中（兼容旧版格式，重新保存，则更新成新版格式）            
    更换<Ctrl+Tab>翻页键成<Tab>
    删除插入中间标号的标签的特性    
    载入资源、文件时提供详细错误信息
    修正，修改文件后，保存-提示有修改是否保存-否-误提示“保存成功”
    -
    PS脚本部分:
    增加默认字体、字号选择功能
    增加导入图片选择功能
    增加导入分组选择功能（建议by 百式）
    增加输出文件夹选择功能
    增加文本替换功能，可批量替换指定字符串
    增加“不对图层进行分组”功能
    增加输出横排文字功能（建议by 超级叫了）
    增加存取配置功能，可保存适用各种场合的配置文件
    优化执行分组动作功能，摆脱对动作库的依赖，动作名称改为分组名（而不是分组编号）

v1.0 preview
    完善了自动设置格式功能，用户仅需简单配置即可使用
    增加QuickText功能，在文本框内Alt+A即可快速输入预设短语
    增加V键临时隐藏标签功能（建议by Pxk888）
    输出ps脚本时，可设置图源格式（如.psd）    （建议by 百式）
    增强多语言支持

v1.0 beta3
    增加标签分类支持，输出ps脚本自动将图层分组
    增加竖屏模式(by sgqy)
    增加插入中间序号的标签功能（插入标签时，取代选中项的序号）
    改善添加删除标签时，鼠标点击的灵敏度（建议by优子）
    改善用户界面友好度(建议by超级叫了)
    拆分工具栏为：主工具栏和标签操作工具栏
    将ps脚本源文件做成外部资源，便于用户自行修改
    修正：ps脚本中，文本顺序倒置的问题（建议by misaki）
    修正：换页索引不归零的bug
    修正：默认导出ps脚本字体过小的问题
    修正：点击标号不存在的位置时，选中标号置空的问题
    修正：修改字体大小导致用户界面崩溃的问题

v1.0 beta2
    增加“编辑标签模式”（快捷键Alt+E），其作用相当于在图片显示区一直按下Ctrl（便于添加、删除标签）
    增强工具易用性：新建时选择工作文件夹，直接保存txt文件，而后开启图片管理窗口
    加入紧急回避功能（Alt+Q），最小化到托盘
    自动备份功能改进，在工作目录下的bak文件夹中，以时间命名自动保存。
    将脚本生成的psd文件名后缀，由"xxx.jpg.psd"/"xxx.png.psd"改为"xxx.psd"/"xxx.psd"
    增加左手翻页快捷键（Ctrl+Tab），增加右手鼠标中键翻页
    增加导出图片进度条（by小学生）
    修正导出图片时，资源未及时释放，内存占用过大的问题（by小学生）
    修正：生成的ps脚本的正文格式取消仿粗体，样式改为平滑
    修正：标签记录位置改为标签中心，确保导出ps脚本输出位置正确

v1.0 beta
    重构程序框架，提升效率以及稳定性
    输出图片时允许选择JPG或PNG
    txt文件中，坐标精精确至三位小数
    更改界面图标
    增加教学例子
    增加输入框中的快捷键：Ctrl+上下左右，用于快速翻页和切换标签
    支持左键点击标签，切换标签    
    修正一个拖动时意外脱出边界的问题
    
v0.3
    增加多语言支持
    增加修改后提示保存功能
    增加30s自动备份功能    

    增强ps脚本输出功能，增加若干可选项(其中更改字体功能在测试中并不总能起作用而原因未明)
    ps脚本自动将文字设为竖排
    优化界面：增加关联图片管理窗口，未选中标签时禁止录入，增加若干处文字提示
    
    修正导出图片时，若文件已存在会导致失败的问题
    修正导出图片时，若当前界面缩放比例不为1.0时，会导致输出图片不正确的问题
    修正若干稳定性问题

v0.2
    增加了ps脚本输出功能
    增加鼠标指向标号时，显示对应文本内容的功能
    修正多次新建文件产生的错误

v0.1
    初版
